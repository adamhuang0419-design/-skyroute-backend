services:
  - type: web
    name: skyroute-backend
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: skyroute-db
          property: connectionString

databases:
  - name: skyroute-db
    plan: free
